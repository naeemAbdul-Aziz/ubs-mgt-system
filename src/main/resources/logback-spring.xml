<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Define log patterns -->
    <!-- Pattern includes MDC variables: %X{traceId} and %X{accountId} -->
    <property name="CONSOLE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%15.15t] %cyan(%-40.40logger{39}) : %m%n%ex" />
    <property name="FILE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%t] [traceId=%X{traceId:-SYSTEM}] [accountId=%X{accountId:-PUBLIC}] %logger{39} : %m%n%ex" />

    <!-- Console Appender (Colorized, easy to read for local dev) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%15.15t] [trace=%X{traceId:-sys}] %cyan(%-40.40logger{39}) : %m%n</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- File Appender (JSON or structured text for production log aggregators) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/ubs-lmis.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- daily rollover -->
            <fileNamePattern>logs/ubs-lmis.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- keep 30 days' worth of history capped at 3GB total size -->
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- Project-specific logging levels -->
    <logger name="com.drakalabs.schoolmngsys" level="DEBUG" />
    
    <!-- Third-party logging levels -->
    <logger name="org.springframework" level="INFO" />
    <logger name="org.hibernate.SQL" level="INFO" /> <!-- Set to DEBUG to see SQL -->
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" /> <!-- See SQL parameters -->

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>
