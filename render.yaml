services:
  # Spring Boot Backend Service
  - type: web
    name: ubs-backend
    env: docker
    plan: free
    region: frankfurt
    dockerfilePath: ./Dockerfile
    healthCheckPath: /actuator/health
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: DATABASE_HOST
        fromDatabase:
          name: ubs-db
          property: host
      - key: DATABASE_PORT
        fromDatabase:
          name: ubs-db
          property: port
      - key: DATABASE_NAME
        fromDatabase:
          name: ubs-db
          property: database
      - key: DATABASE_USERNAME
        fromDatabase:
          name: ubs-db
          property: user
      - key: DATABASE_PASSWORD
        fromDatabase:
          name: ubs-db
          property: password
      - key: JWT_SECRET
        generateValue: true
    
databases:
  - name: ubs-db
    plan: free
    region: frankfurt
    databaseName: ubs_prod
    user: ubs_admin
